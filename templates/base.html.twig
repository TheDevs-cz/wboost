<!DOCTYPE html>
<html lang="{{ app.request.locale }}" data-turbo="false">
    <head>
        <meta charset="UTF-8">
        <title>{% apply spaceless %}{% block title %}{% endblock %}{% endapply %} | WBoost</title>

        {% block meta_viewport %}
            <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
        {% endblock %}

        <meta name="turbo-prefetch" content="false">
        <meta name="turbo-refresh-method" content="morph">

        <meta name="description" content="{% apply spaceless %}{% block meta_description %}{% endblock %}{% endapply %}">
        <meta name="keywords" content="{% apply spaceless %}{% block meta_keywords %}{% endblock %}{% endapply %}">

        <meta name="msapplication-TileColor" content="#ffffff">
        <meta name="theme-color" content="#ffffff">

        <link rel="apple-touch-icon" sizes="57x57" href="{{ asset('apple-icon-57x57.png') }}">
        <link rel="apple-touch-icon" sizes="60x60" href="{{ asset('apple-icon-60x60.png') }}">
        <link rel="apple-touch-icon" sizes="72x72" href="{{ asset('apple-icon-72x72.png') }}">
        <link rel="apple-touch-icon" sizes="76x76" href="{{ asset('apple-icon-76x76.png') }}">
        <link rel="apple-touch-icon" sizes="114x114" href="{{ asset('apple-icon-114x114.png') }}">
        <link rel="apple-touch-icon" sizes="120x120" href="{{ asset('apple-icon-120x120.png') }}">
        <link rel="apple-touch-icon" sizes="144x144" href="{{ asset('apple-icon-144x144.png') }}">
        <link rel="apple-touch-icon" sizes="152x152" href="{{ asset('apple-icon-152x152.png') }}">
        <link rel="apple-touch-icon" sizes="180x180" href="{{ asset('apple-icon-180x180.png') }}">
        <link rel="icon" type="image/png" sizes="192x192"  href="{{ asset('android-icon-192x192.png') }}">
        <link rel="icon" type="image/png" sizes="32x32" href="{{ asset('favicon-32x32.png') }}">
        <link rel="icon" type="image/png" sizes="96x96" href="{{ asset('favicon-96x96.png') }}">
        <link rel="icon" type="image/png" sizes="16x16" href="{{ asset('favicon-16x16.png') }}">
        <link rel="manifest" href="{{ asset('manifest.json') }}">
        <meta name="msapplication-TileColor" content="#ffffff">
        <meta name="msapplication-TileImage" content="{{ asset('ms-icon-144x144.png') }}">
        <meta name="theme-color" content="#ffffff">

        <link rel="shortcut icon" type="image/x-icon" href="{{ asset('favicon.ico') }}">

        <link type="text/plain" rel="author" href="{{ asset('humans.txt') }}">

        {% block importmap %}
            {{ importmap('app') }}
        {% endblock %}


        <!-- Theme Config Js -->
        <script src="{{ asset('theme/js/hyper-config.js') }}"></script>

        <!-- App css -->
        <link href="{{ asset('theme/css/app-saas.min.css') }}" rel="stylesheet" type="text/css" id="app-style" />

        <!-- Icons css -->
        <link href="{{ asset('theme/css/icons.min.css') }}" rel="stylesheet" type="text/css" />

        {% block custom_styles %}{% endblock %}
    </head>
    <body class="{% block body_class %}{% endblock %}" data-controller="confirm-modal">
    {% block full_content %}
        <!-- Begin page -->
        <div class="wrapper">

            <!-- ========== Left Sidebar Start ========== -->
            <div class="leftside-menu">

                <!-- Brand Logo Light -->
                <a href="{{ path('homepage') }}" class="logo logo-light">
                    <span class="logo-lg">
                        <img src="{{ asset('images/logo-lg.svg') }}" alt="logo">
                    </span>
                    <span class="logo-sm">
                        <img src="{{ asset('images/logo-icon.svg') }}" alt="small logo">
                    </span>
                </a>

                <!-- Brand Logo Dark -->
                <a href="{{ path('homepage') }}" class="logo logo-dark">
                    <span class="logo-lg">
                        <img src="{{ asset('images/logo-lg.svg') }}" alt="dark logo">
                    </span>
                    <span class="logo-sm">
                        <img src="{{ asset('images/logo-icon.svg') }}" alt="small logo">
                    </span>
                </a>

                <!-- Sidebar Hover Menu Toggle Button -->
                <div class="button-sm-hover" data-bs-toggle="tooltip" data-bs-placement="right" title="Show Full Sidebar">
                    <i class="ri-checkbox-blank-circle-line align-middle"></i>
                </div>

                <!-- Full Sidebar Menu Close Button -->
                <div class="button-close-fullsidebar">
                    <i class="ri-close-fill align-middle"></i>
                </div>

                <!-- Sidebar -left -->
                <div class="h-100" id="leftside-menu-container" data-simplebar>
                    <!--- Sidemenu -->
                    <ul class="side-nav">
                        <li class="side-nav-item {{ active_menu_item|default == 'projects' ? 'menuitem-active' }}">
                            <a href="{{ path('projects') }}" class="side-nav-link">
                                <i class="uil-home-alt"></i>
                                <span> Projekty </span>
                            </a>
                        </li>

                        <li class="side-nav-item {{ active_menu_item|default == 'mcp_guide' ? 'menuitem-active' }}">
                            <a href="{{ path('mcp_guide') }}" class="side-nav-link">
                                <i class="uil-robot"></i>
                                <span> AI (MCP server) </span>
                            </a>
                        </li>

                        {% if is_granted('ROLE_ADMIN') %}
                            <li class="side-nav-title">Administrace</li>

                            <li class="side-nav-item {{ active_menu_item|default == 'admin_users' ? 'menuitem-active' }}">
                                <a href="{{ path('admin_users') }}" class="side-nav-link">
                                    <i class="uil-users-alt"></i>
                                    <span> Uživatelé </span>
                                </a>
                            </li>

                            <li class="side-nav-item {{ active_menu_item|default == 'admin_registration_requests' ? 'menuitem-active' }}">
                                <a href="{{ path('admin_registration_requests') }}" class="side-nav-link">
                                    <i class="uil-user-plus"></i>
                                    <span> Žádosti o registraci </span>
                                    {% set pendingRegistrationRequests = pending_registration_requests_count() %}
                                    {% if pendingRegistrationRequests > 0 %}
                                        <span class="badge bg-danger rounded-pill">{{ pendingRegistrationRequests }}</span>
                                    {% endif %}
                                </a>
                            </li>

                            <li class="side-nav-item {{ active_menu_item|default == 'admin_usage' ? 'menuitem-active' }}">
                                <a href="{{ path('admin_usage') }}" class="side-nav-link">
                                    <i class="uil-chart-line"></i>
                                    <span> Statistiky </span>
                                </a>
                            </li>
                        {% endif %}

                        {% if project is defined %}
                            {% set nav_avatar = project_avatar(project) %}
                            <li class="side-nav-title d-flex align-items-center">
                                {{ include('_project_avatar.html.twig', {'avatar': nav_avatar, 'size': 22, 'font_size': '.6rem', 'alt': ''}) }}
                                <span class="text-truncate ms-1">{{ project.name }}</span>
                            </li>

                            <li class="side-nav-item {{ active_menu_item|default == 'manuals' ? 'menuitem-active' }}">
                                <a href="{{ path('manuals_list', {'id': project.id}) }}" class="side-nav-link">
                                    <i class="uil-copy-alt"></i>
                                    <span> Manuály </span>
                                </a>
                            </li>

                            <li class="side-nav-item {{ active_menu_item|default == 'templates' ? 'menuitem-active' }}">
                                <a href="{{ path('templates', {'projectId': project.id}) }}" class="side-nav-link">
                                    <i class="uil-postcard"></i>
                                    <span> Šablony </span>
                                </a>
                            </li>

                            {% if is_granted('project_edit', project) %}
                                <li class="side-nav-item {{ active_menu_item|default == 'gallery' ? 'menuitem-active' }}">
                                    <a href="{{ path('project_gallery', {'projectId': project.id}) }}" class="side-nav-link">
                                        <i class="uil-images"></i>
                                        <span> Galerie </span>
                                    </a>
                                </li>
                            {% endif %}

                            <li class="side-nav-item {{ active_menu_item|default == 'calendars' ? 'menuitem-active' }}">
                                <a href="{{ path('calendars', {'projectId': project.id}) }}" class="side-nav-link">
                                    <i class="uil-calendar-alt"></i>
                                    <span> Kalendáře </span>
                                </a>
                            </li>

                            <li class="side-nav-item {{ active_menu_item|default == 'fonts' ? 'menuitem-active' }}">
                                <a href="{{ path('fonts_list', {'id': project.id}) }}" class="side-nav-link">
                                    <i class="ri-font-family"></i>
                                    <span> Fonty</span>
                                </a>
                            </li>

                            <li class="side-nav-item {{ active_menu_item|default == 'emails' ? 'menuitem-active' }}">
                                <a href="{{ path('email_signature_templates', {'id': project.id}) }}" class="side-nav-link">
                                    <i class="ri-mail-line"></i>
                                    <span> E-maily</span>
                                </a>
                            </li>

                            <li class="side-nav-item {{ active_menu_item|default == 'weekly_menus' ? 'menuitem-active' }}">
                                <a href="{{ path('weekly_menus', {'projectId': project.id}) }}" class="side-nav-link">
                                    <i class="uil-restaurant"></i>
                                    <span> Jídelníčky</span>
                                </a>
                                {% if active_menu_item|default == 'weekly_menus' %}
                                    <ul class="side-nav-second-level">
                                        <li class="{{ active_submenu_item|default == 'meals' ? 'active' : '' }}">
                                            <a href="{{ path('meals', {'projectId': project.id}) }}">Seznam jídel</a>
                                        </li>
                                        <li class="{{ active_submenu_item|default == 'diets' ? 'active' : '' }}">
                                            <a href="{{ path('diets', {'projectId': project.id}) }}">Diety</a>
                                        </li>
                                        <li class="{{ active_submenu_item|default == 'dish_types' ? 'active' : '' }}">
                                            <a href="{{ path('dish_types', {'projectId': project.id}) }}">Druhy jídel</a>
                                        </li>
                                    </ul>
                                {% endif %}
                            </li>
                        {% endif %}
                    </ul>
                    <!--- End Sidemenu -->

                    <div class="clearfix"></div>
                </div>

                {# Account menu — the topbar it used to live in is gone. It sits
                   OUTSIDE #leftside-menu-container (so the drop-up is never
                   clipped by simplebar) in the padding the sidebar reserves at
                   its bottom edge. #}
                <div class="leftbar-account dropup">
                    {# offset = 12px skidding so the drop-up insets from the window
                       edge instead of sitting flush against it. #}
                    <a href="#" class="leftbar-account__toggle" data-bs-toggle="dropdown" data-bs-offset="12,6" role="button" aria-haspopup="true" aria-expanded="false">
                        <img src="{{ asset('theme/images/users/avatar-1.jpg') }}" alt="" width="32" height="32" class="rounded-circle flex-shrink-0">
                        <span class="leftbar-account__name text-truncate">{{ app.user.displayName }}</span>
                        <i class="mdi mdi-chevron-up leftbar-account__caret"></i>
                    </a>
                    <div class="dropdown-menu dropdown-menu-animated leftbar-account__menu">
                        <!-- item-->
                        <a href="{{ path('user_profile') }}" class="dropdown-item">
                            <i class="mdi mdi-account-circle me-1"></i>
                            <span>Můj účet</span>
                        </a>

                        <!-- item-->
                        <a href="{{ path('connected_apps') }}" class="dropdown-item">
                            <i class="mdi mdi-power-plug me-1"></i>
                            <span>Propojené aplikace</span>
                        </a>

                        <!-- item-->
                        <a href="{{ path('logout') }}" class="dropdown-item">
                            <i class="mdi mdi-logout me-1"></i>
                            <span>Odhlásit se</span>
                        </a>
                    </div>
                </div>
            </div>
            <!-- ========== Left Sidebar End ========== -->

            <!-- ============================================================== -->
            <!-- Start Page Content here -->
            <!-- ============================================================== -->

            <div class="content-page">

                {# Below xl the sidebar collapses (off-canvas under 768px), so the
                   drawer toggle needs a home now that the topbar is gone. The
                   theme binds the FIRST .button-toggle-menu it finds. #}
                <div class="mobile-topbar d-xl-none">
                    <button class="button-toggle-menu" type="button" aria-label="Menu">
                        <i class="mdi mdi-menu"></i>
                    </button>
                    <a href="{{ path('homepage') }}" class="mobile-topbar__logo">
                        <img src="{{ asset('images/logo-lg.svg') }}" alt="logo" height="22">
                    </a>
                </div>

                <div class="content">

                    <!-- Start Content-->
                    <div class="container-fluid">

                        <!-- start page title -->
                        {# Overridable so full-height app screens (the canvas
                           editors) can reclaim the vertical space — their
                           breadcrumb already ends with this exact title. #}
                        {% block page_title_row %}
                        <div class="row">
                            <div class="col-sm-6">
                                <div class="page-title-box">
                                    <h1 class="page-title">{{ block('title') }}</h1>
                                </div>
                            </div>
                            <div class="col-sm-6 pt-0 pt-sm-4">
                                {% block title_actions %}{% endblock %}
                            </div>
                        </div>
                        {% endblock %}
                        <!-- end page title -->

                        {% block breadcrumb %}{% endblock %}

                        {{ include('_flashes.html.twig') }}

                        {% block content %}{% endblock %}
                    </div>
                    <!-- container -->

                </div>
                <!-- content -->

            </div>

            <!-- ============================================================== -->
            <!-- End Page content -->
            <!-- ============================================================== -->

        </div>
        <!-- END wrapper -->
    {% endblock %}

    <!-- Vendor js -->
    <script src="{{ asset('theme/js/vendor.min.js') }}"></script>

    <!-- App js -->
    <script src="{{ asset('theme/js/app.min.js') }}"></script>

    </body>
</html>
