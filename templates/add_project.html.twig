{% extends 'base.html.twig' %}

{% block title %}Nový projekt{% endblock %}
{% set active_menu_item = 'projects' %}

{% block breadcrumb %}
    <ol class="breadcrumb m-0 pt-0">
        <li class="breadcrumb-item"><i class="uil-home-alt"></i> <a href="{{ path('homepage') }}">Projekty</a></li>
        <li class="breadcrumb-item active">{{ block('title') }}</li>
    </ol>
{% endblock %}

{% block content %}
    <div class="card">
        <div class="card-body">
            {{ form_start(form) }}
            {{ form_row(form.name) }}

            <input type="submit" name="submit" value="Vytvořit" class="btn btn-primary">

            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
